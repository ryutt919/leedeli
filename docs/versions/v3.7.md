# Project Update Report - v3.7

## 📌 Version Summary
프렙 페이지 UI 레이아웃 개선 및 비용 표시 정수 최적화.

## 💬 User Requirements
- [x] 달력 내 프렙 태그를 날짜 밑에 왼쪽 정렬로 세로 쌓기
- [x] 프렙 이름을 최대 4글자로 제한
- [x] 비용 표시 시 소수점 제거 및 정수형으로 표시 (`원` 추가)
- [x] 모바일에서 DatePicker 입력 시 키보드 미노출 설정
- [x] 깃허브 강제 푸시를 통한 과거 시점(375d650) 복구 및 동기화

## 🛠 Key Changes
### 1. 코드 수정 (Logic)
- `PrepsPage.tsx`:
  - `dateCellRender`: 태그 컨테이너에 `display: flex`, `flexDirection: column`, `alignItems: flex-start` 추가.
  - 이름 길이 제한 로직: `p.name.length > 4 ? p.name.slice(0, 4) : p.name` 적용.
  - 비용 표시: `Math.round(calcPrepCost(p))` 및 `Math.round(calcCostFromFormItems(items))` 적용.
  - `DatePicker`: `inputReadOnly` 속성 추가.

### 2. 설정 변경 (Config)
- `package.json` 버전 유지 (`0.0.0`)
- `index.css`: 커서 깜빡임 제거 스타일 통합 (`375d650` 복구 상태 유지).

## ✅ Verification Results
- **Test Case 1**: 달력 내 동일 날짜 다수 프렙 존재 시 왼쪽 정렬 세로 배치 확인.
- **Test Case 2**: 프렙 수정 및 리스트 화면에서 비용이 정수로 반올림되어 표시되는지 확인.
- **Test Case 3**: `npm run build` 수행 결과 오류 없음 확인.

## 📝 Todo & History
- [x] #todo: 달력 태그 세로 배치 적용 -> **(v3.7 fixed)** 완료.
- [x] #todo: 비용 정수 표시 -> **(v3.7 fixed)** 완료.
