# Project Update Report - v4.1

## 📌 Version Summary
Supabase 인증 연동(로그인 필수/유지), 프렙 보충 이력에 작업자 이름 기록, 스케줄 페이지 UI/UX 테마 통합.

## 💬 User Requirements
- [x] Supabase 기반 로그인/회원가입 기능 추가 (이름 입력 포함)
- [x] 로그인하지 않으면 모든 페이지 접근 차단 + 로그인 유지
- [x] 프렙 보충 시 '누가 보충했는지' 이름으로 DB에 기록 및 UI 표시
- [x] 스케줄 생성·관리 페이지의 레이아웃을 MobileShell 테마로 통합

## 🛠 Key Changes

### 1. 코드 수정 (Logic)

| 파일 | 변경 내용 |
|---|---|
| `App.tsx` | `RequireAuth` 래퍼로 전체 라우트 보호, `/login` 라우트 추가 |
| `LoginPage.tsx` | **[NEW]** 네온 그린 다크 테마 로그인/회원가입 페이지, 회원가입 시 이름 입력 → `user_metadata.name` 저장 |
| `RequireAuth.tsx` | **[NEW]** 세션 없으면 `/login`으로 리다이렉셔 + 실시간 세션 감지 |
| `supabase.ts` | **[NEW]** Supabase 클라이언트 초기화 (세션 자동 유지) |
| `restockRepo.ts` | **[NEW]** Supabase `restock_history` 테이블 CRUD 유틸리티 (이름 기록) |
| `domain/types.ts` | `RestockRecord` 타입 추가 |
| `PrepsPage.tsx` | 보충 추가/삭제 시 Supabase에도 기록, 날짜 클릭 모달에서 보충자 이름 태그 표시 |
| `CreateSchedulePage.v2.tsx` | `MobileShell` 래퍼 적용, 독자 `<h1>` 제거 |
| `ManageSchedulesPage.v2.tsx` | `MobileShell` 래퍼 적용, 독자 스타일 제거 |

### 2. 설정 변경 (Config)
- `package.json`: `@supabase/supabase-js` 의존성 추가
- `.env.local`: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 환경변수 설정

### 3. Pipeline
- Supabase DB에 `ingredients`, `preps`, `prep_items`, `restock_history` 4개 테이블 생성

## ✅ Verification Results
- **tsc --noEmit**: 타입 빌드 오류 0건
- **브라우저 테스트**: 미인증 접속 시 `/login` 리다이렉션 정상, 회원가입 폼에 이름 필드 표시 확인
- **UI 통합**: 스케줄 생성/관리 페이지가 MobileShell 레이아웃(상단 헤더 + 하단 내비게이션)으로 통합됨

## 📝 Todo & History
- #todo: Vercel 재배포 후 실서비스 환경에서 로그인/보충 이력 공유 테스트 필요
- #todo: 재료(Ingredients) 데이터도 Supabase로 마이그레이션 검토
