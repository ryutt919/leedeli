---
noteId: "947f51f02f0511f18b765d8e8a841ff3"
tags: []

---

# Project Update Report - v4.2

## 📌 Version Summary

인증 과정의 무한 로딩 해결 및 재료/프렙 추가 안정성 개선.

## 💬 User Requirements

- [X] 스케줄 생성 화면 무한 로딩 현상 해결
- [X] 재료 추가 기능 실패 문제 해결
- [X] 프렙 추가 기능 실패 문제 해결

## 🛠 Key Changes

### 1. 코드 수정 (Logic)

- `src/components/RequireAuth.tsx`: `AuthContext`의 `useAuth`를 사용하여 중복된 세션 체크를 제거하고 로딩 상태를 통합 관리합니다.
- `src/auth/AuthContext.tsx`: `fetchIsAdmin` 함수에 5초 타임아웃을 추가하여 Supabase 응답 지연 시에도 `loading` 상태가 해제되도록 개선했습니다.
- `src/storage/prepsRepo.ts`: Supabase 연동 시 발생하는 오류를 `throw`하도록 수정하여 호출부에서 실패를 감지할 수 있게 했습니다.
- `src/pages/PrepsPage.tsx`: `onSave` 함수에서 저장 실패 시 사용자에게 알림을 표시하고 모달을 유지하도록 예외 처리를 추가했습니다.
- `src/pages/IngredientsPage.tsx`: `onSave` 함수에 예외 처리를 추가하여 로컬 스토리지 저장 오류 시에도 사용자에게 상태를 알립니다.

### 2. 설정 변경 (Config)

- 인증 로직 통합으로 인한 라우팅 안정성 확보.

## ✅ Verification Results

- **Test Case 1 (Mock Auth):** 모킹된 로그인 세션을 사용하여 `CreateSchedulePage`가 정상 로드됨을 확인.
- **Test Case 2 (Ingredients):** 재료 추가 시 성공 메시지와 함께 리스트에 즉시 반영됨을 확인.
- **Test Case 3 (Preps):** 프렙 추가 시 Supabase 연동 오류 발생 상황에서 저장 실패 메시지가 정상 노출됨을 확인.

## 📝 Todo & History

- #todo: 재료(Ingredients) 데이터도 Supabase로 마이그레이션 검토 (진행 중)
- #feat -> [인증 무한 로딩 및 저장 실패 예외 처리 도입으로 안정성 강화].
